# DKAN Client Tools - Base Environment Configuration
#
# DO NOT manually create this file!
# The .env file is auto-generated during DKAN setup with secure API credentials.
#
# To create .env with API credentials:
#   cd dkan
#   ddev start
#
# The automated setup process will:
#   1. Create a dedicated API user (dkan-api-user) in Drupal
#   2. Generate a secure random 32-character password
#   3. Save credentials to dkan/.env
#   4. Copy .env to project root for use by Node.js scripts
#
# ==================================================================================
# MULTI-ENVIRONMENT SUPPORT:
# This project supports multiple environments using NODE_ENV:
#
#   .env                      # Base config (this file - auto-generated)
#   .env.local                # Personal overrides (gitignored)
#   .env.development.example  # Development template
#   .env.test.example         # Test environment template
#   .env.production.example   # Production template
#
# To configure a specific environment, copy the example and customize:
#   cp .env.test.example .env.test.local
#   # Edit .env.test.local with your test server credentials
#
# Usage:
#   npm run record:api                    # Uses .env (development)
#   NODE_ENV=test npm run record:api      # Uses .env.test.local
#   NODE_ENV=production npm run record:api # Uses .env.production.local
#
# ==================================================================================
# SECURITY NOTICE:
# - .env is gitignored and should NEVER be committed to version control
# - API credentials are separate from admin login
# - Admin password can be anything (default: 'admin' for local dev)
# - API credentials are auto-rotated when you run: ddev drush dkan-api-user --regenerate
# ==================================================================================

# DKAN site URL
DKAN_URL="https://dkan.ddev.site"

# DKAN API credentials (auto-generated - do not edit manually)
# Generated by: ddev drush dkan-client:create-api-user
# Values are quoted to handle special characters in passwords
DKAN_USER="dkan-api-user"
DKAN_PASS="<auto-generated-32-character-password>"

# Recording mode (optional)
# Set to "true" to skip mutation operations (create/update/delete)
# Default: true if DKAN_USER is not set, false otherwise
READ_ONLY=false

# Cleanup mode (optional)
# Set to "true" to only perform cleanup operations without creating new records
# Default: false
CLEANUP_ONLY=false
