# DKAN Client Tools - Production Environment
#
# This file shows configuration for production servers.
# Copy to .env.production.local and add your production credentials.
#
# SECURITY: Never commit .env.production or .env.production.local!
# Use secrets manager (GitHub Secrets, AWS Secrets Manager, etc.) in CI/CD.
#
# Usage:
#   NODE_ENV=production npm run record:api

# DKAN production server URL
DKAN_URL="https://data.example.com"

# DKAN API credentials
# DO NOT put real credentials here - use .env.production.local (gitignored)
# or environment variables in CI/CD
DKAN_USER="<get-from-secrets-manager>"
DKAN_PASS="<get-from-secrets-manager>"

# ALWAYS use read-only mode in production
READ_ONLY=true

# Normal operation (not cleanup-only mode)
CLEANUP_ONLY=false

# Explicitly set environment
NODE_ENV=production
