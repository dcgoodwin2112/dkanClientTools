# DKAN Client Tools - Test Environment
#
# This file shows configuration for test/staging servers.
# Copy to .env.test.local and add your test server credentials.
#
# SECURITY: Never commit .env.test or .env.test.local to version control!
# Use secrets manager (GitHub Secrets, AWS Secrets Manager, etc.) in CI/CD.
#
# Usage:
#   NODE_ENV=test npm run record:api

# DKAN test server URL
DKAN_URL="https://test.example.com"

# DKAN API credentials
# DO NOT put real credentials here - use .env.test.local (gitignored)
# or environment variables in CI/CD
DKAN_USER="<get-from-secrets-manager>"
DKAN_PASS="<get-from-secrets-manager>"

# Enable read-only mode for safety (recommended for test)
READ_ONLY=true

# Normal operation (not cleanup-only mode)
CLEANUP_ONLY=false

# Explicitly set environment
NODE_ENV=test
